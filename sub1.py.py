import mainmodule as mm

a = mm.person1['age']
print(a)
#3
#-The purpose of "as" is, it takes the imported module and renames it to whichever the user has renamed it to be, overmore,
#it makes the imported module as a variable in the script.
